{
  "name": "hugo-discussion-bridge",
  "version": "0.1.0-alpha.3",
  "description": "Build-time DiscussionBridge adapter for Hugo",
  "type": "module",
  "bin": { "discussionbridge-hugo": "./src/cli.mjs" },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/adapter.mjs && node --check src/cli.mjs"
  },
  "engines": { "node": ">=22" },
  "dependencies": { "sanitize-html": "2.17.0" }
}
